import '@styles/tailwind.css';
import Head from 'next/head';
import { useRouter } from 'next/router';
import { ProviderAuth } from '@hooks/use-auth';
import Header from '@containers/Header';

function MyApp({ Component, pageProps }) {
  const router = useRouter();

  return (
    <ProviderAuth>
      <Head>
        <title>Stream Play</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossOrigin={true} />
        <link href="https://fonts.googleapis.com/css2?family=Abril+Fatface&family=Arvo&family=PT+Serif&family=Pacifico&display=swap" rel="stylesheet" />
      </Head>
      <div className="flex flex-col pt-[63px]">
        {router.pathname == '/signup' || router.pathname == '/login' ? null : <Header backButton={router.pathname != '/' ? true : false} />}
        <main>
          <Component {...pageProps} />
        </main>
      </div>
    </ProviderAuth>
  );
}

export default MyApp;
